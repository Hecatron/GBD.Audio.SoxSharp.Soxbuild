# Gbd.Audio.SoxSharp.SoxBuild

## Overview

.Net Wrapper Library for libsox audio conversion and manipulation

This code base is a basis for the build of libsox as a dll / dynamic library using swig as a wrapper
